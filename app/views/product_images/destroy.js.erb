$('[data-id="<%= @product_image.id %>"]').remove();
var $product_images_count = $('.form-product-images').attr('data-images-count') || 0
$('.form-product-images').attr('data-images-count', parseInt($product_images_count) - 1);